import React from "react";

//components
import HeaderPanel from "./HeaderPanel";

const Layout = () => {
  return (
    <React.Fragment>
      <HeaderPanel />
    </React.Fragment>
  );
};

export default Layout;
